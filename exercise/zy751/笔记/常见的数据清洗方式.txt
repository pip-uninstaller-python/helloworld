strip()  将字符串俩边的空格去掉，可在里面添加参数，去掉俩边指定的参数
例子：a=‘aabbbbaa’
	a=a.strip(‘a’)  则a为‘bb’
		
split（）将一段字符串以指定的内容分割开来，返回结果为列表
例子：a=‘a,b,c’
	a=a.split(',') 则a为['a','b','c']
join（）可将列表中元素以指定内容连接，返回结果为字符串
例子: a=['a','b','c']
	b=','
	a=b.join(a) 则a为‘a,b,c’
总结：一般字符串操作后要重新赋值，原数据不会直接改变