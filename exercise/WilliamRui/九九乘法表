#打印九九乘法表
for i in range(1,10):
#外层循环控制行数，共九行，所以是1 - 10
    for j in range(1,i+1):
    #内层循环控制列数，第几行就有几列，所以是i列，1 - i+1
        print('{}x{}={:<2} '.format(j,i,i*j),end='')
        #':<2'控制输出为两位，左对齐，由于print语句输出后会自动换行，所以用end=‘’让语句结束后不自动换行
    print('')
    #每行输出完成后换行。
